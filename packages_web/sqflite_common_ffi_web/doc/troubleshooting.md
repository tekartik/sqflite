## sqlite3 Troubleshooting

sqlite3 v3 is supported since v1.1.0

```yaml
dependencives:
  sqflite_common_ffi_web: ^1.1.0
```

If you want to remain on sqlite3 v2 (pre 3.0.0) you can use:

```yaml
dependencives:
  sqflite_common_ffi_web: ^1.0.2
```
